####################################################################################################################################
# aws.sh
# - A wrapper around the Docker implementation of the AWS CLI 
####################################################################################################################################

docker run -it --rm  -v ~/.aws:/root/.aws -v $(pwd):/aws -e AWS_PROFILE amazon/aws-cli:latest "$@"